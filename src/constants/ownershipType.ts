export enum OwnershipType {
    OWNER_AND_SHARED_BY_LAST_OPENED = 'ownerAndSharedByLastOpened',
    ALL_ROOT = 'allRoot',
    OWNER_AND_SHARED = 'ownerAndShared',
    OWNER = 'owner',
    SHARED_ROOT = 'sharedRoot',
    SHARED = 'shared',
}
